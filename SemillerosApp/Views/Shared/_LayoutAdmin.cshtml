@{
    var userData = (User.Identity as System.Web.Security.FormsIdentity)?.Ticket.UserData ?? "";
    var partes = userData.Split('|');
    var nombreAdm = partes.Length >= 2 ? partes[1] : "Admin";
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title - Panel Admin</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    <style>
        :root {
            --primary: #1a5276;
            --secondary: #2e86c1;
            --accent: #28b463;
            --sidebar-w: 260px;
            --top-h: 64px;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #f0f4f8;
            display: flex;
            min-height: 100vh;
        }

        /* ── SIDEBAR ── */
        .sidebar {
            width: var(--sidebar-w);
            background: linear-gradient(180deg, #0d1b2a 0%, var(--primary) 100%);
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            z-index: 1000;
            transition: transform .3s;
        }

        .sidebar-brand {
            padding: 22px 20px;
            border-bottom: 1px solid rgba(255,255,255,.1);
            display: flex;
            align-items: center;
            gap: 12px;
        }

            .sidebar-brand .brand-icon {
                width: 42px;
                height: 42px;
                background: linear-gradient(135deg,var(--secondary),var(--accent));
                border-radius: 10px;
                display: flex;
                align-items: center;
                justify-content: center;
            }

                .sidebar-brand .brand-icon i {
                    font-size: 20px;
                    color: #fff;
                }

            .sidebar-brand span {
                color: #fff;
                font-weight: 700;
                font-size: .95rem;
                line-height: 1.2;
            }

            .sidebar-brand small {
                color: rgba(255,255,255,.55);
                font-size: .7rem;
                display: block;
            }

        .sidebar-nav {
            flex: 1;
            overflow-y: auto;
            padding: 14px 0;
        }

        .nav-section-title {
            color: rgba(255,255,255,.35);
            font-size: .64rem;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            padding: 12px 22px 6px;
        }

        .nav-item a {
            display: flex;
            align-items: center;
            gap: 12px;
            color: rgba(255,255,255,.7);
            text-decoration: none;
            padding: 10px 22px;
            font-size: .85rem;
            border-radius: 0;
            transition: background .2s, color .2s;
            position: relative;
        }

            .nav-item a i {
                width: 18px;
                text-align: center;
                font-size: .9rem;
            }

            .nav-item a:hover {
                background: rgba(255,255,255,.08);
                color: #fff;
            }

            .nav-item a.active {
                background: linear-gradient(90deg, rgba(46,134,193,.35), transparent);
                color: #fff;
                font-weight: 600;
            }

                .nav-item a.active::before {
                    content: '';
                    position: absolute;
                    left: 0;
                    top: 0;
                    bottom: 0;
                    width: 3px;
                    background: var(--secondary);
                    border-radius: 0 3px 3px 0;
                }

        .sidebar-footer {
            padding: 16px 20px;
            border-top: 1px solid rgba(255,255,255,.1);
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
        }

        .user-avatar {
            width: 36px;
            height: 36px;
            background: linear-gradient(135deg,var(--secondary),var(--accent));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-weight: 700;
            font-size: .85rem;
            flex-shrink: 0;
        }

        .user-name {
            color: #fff;
            font-size: .8rem;
            font-weight: 600;
        }

        .user-role {
            color: rgba(255,255,255,.45);
            font-size: .68rem;
        }

        /* ── TOPBAR ── */
        .main-wrap {
            margin-left: var(--sidebar-w);
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .topbar {
            height: var(--top-h);
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 28px;
            box-shadow: 0 2px 8px rgba(0,0,0,.07);
            position: sticky;
            top: 0;
            z-index: 500;
        }

        .topbar-title {
            font-size: 1.05rem;
            font-weight: 600;
            color: var(--primary);
        }

            .topbar-title small {
                font-size: .75rem;
                color: #7f8c8d;
                display: block;
                font-weight: 400;
            }

        .topbar-right {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .btn-logout {
            background: linear-gradient(135deg,#e74c3c,#c0392b);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 7px 16px;
            font-size: .8rem;
            font-family: 'Poppins',sans-serif;
            font-weight: 500;
            cursor: pointer;
            transition: opacity .2s;
        }

            .btn-logout:hover {
                opacity: .88;
            }

        /* ── CONTENT ── */
        .content {
            padding: 28px;
            flex: 1;
        }

        /* ── TOAST ── */
        .toast-container {
            position: fixed;
            top: 80px;
            right: 22px;
            z-index: 9999;
        }
    </style>
    @RenderSection("Styles", required: false)
</head>


<body>

    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="sidebar-brand">
            <div class="brand-icon"><i class="fas fa-seedling"></i></div>
            <div>
                <span>SemillerosApp</span>
                <small>Panel de Administrador</small>
            </div>
        </div>

        <nav class="sidebar-nav">
            <div class="nav-section-title">Principal</div>
            <div class="nav-item">
                <a href="@Url.Action("Index","AdminDashboard")"
                   class="@(currentController=="AdminDashboard" && currentAction=="Index" ? "active" : "")">
                    <i class="fas fa-th-large"></i> Dashboard
                </a>
            </div>

            <div class="nav-section-title">Gestión</div>
            <div class="nav-item">
                <a href="@Url.Action("Semilleros","AdminDashboard")"
                   class="@(currentAction=="Semilleros" || currentAction=="CrearSemillero" || currentAction=="EditarSemillero" ? "active" : "")">
                    <i class="fas fa-leaf"></i> Semilleros
                </a>
            </div>
            <div class="nav-item">
                <a href="@Url.Action("Usuarios","AdminDashboard")"
                   class="@(currentAction=="Usuarios" || currentAction=="AgregarUsuario" || currentAction=="EditarUsuario" ? "active" : "")">
                    <i class="fas fa-users"></i> Usuarios
                </a>
            </div>
            <div class="nav-item">
                <a href="@Url.Action("Eventos","AdminDashboard")"
                   class="@(currentAction=="Eventos" || currentAction=="DetalleEvento" ? "active" : "")">
                    <i class="fas fa-calendar-alt"></i> Eventos
                </a>
            </div>
            <div class="nav-item">
                <a href="@Url.Action("Reuniones","AdminDashboard")"
                   class="@(currentAction=="Reuniones" || currentAction=="CrearReunion" || currentAction=="EditarReunion" ? "active" : "")">
                    <i class="fas fa-handshake"></i> Reuniones
                </a>
            </div>

            <div class="nav-section-title">Herramientas</div>
            <div class="nav-item">
                <a href="@Url.Action("Index","Asistente")"
                   class="@(currentController=="Asistente" ? "active" : "")">
                    <i class="fas fa-robot"></i> Asistente IA
                </a>
            </div>

            <div class="nav-section-title">Cuenta</div>
            <div class="nav-item">
                <a href="@Url.Action("MiPerfil","AdminDashboard")"
                   class="@(currentAction=="MiPerfil" ? "active" : "")">
                    <i class="fas fa-user-circle"></i> Mi Perfil
                </a>
            </div>
        </nav>

        <div class="sidebar-footer">
            <div class="user-info">
                <div class="user-avatar">@nombreAdm.Substring(0, 1).ToUpper()</div>
                <div>
                    <div class="user-name">@nombreAdm</div>
                    <div class="user-role">Administrador</div>
                </div>
            </div>
            @using (Html.BeginForm("Logout", "Account", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <button type="button" class="btn-logout w-100" onclick="confirmarLogout()">
                    <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                </button>
            }
        </div>
    </aside>

    <!-- MAIN -->
    <div class="main-wrap">
        <header class="topbar">
            <div class="topbar-title">
                @ViewBag.Title
                <small>@ViewBag.Subtitle</small>
            </div>
            <div class="topbar-right">
                <span style="font-size:.8rem;color:#7f8c8d">
                    <i class="fas fa-calendar-alt me-1"></i>
                    @DateTime.Now.ToString("dd/MM/yyyy")
                </span>
            </div>
        </header>

        <!-- Toast de notificaciones -->
        @if (TempData["Mensaje"] != null)
        {
            <div class="toast-container">
                <div class="toast show align-items-center text-white
                         bg-@(TempData["TipoMensaje"]?.ToString() == "success" ? "success" : "warning")"
                     role="alert">
                    <div class="d-flex">
                        <div class="toast-body">
                            <i class="fas fa-@(TempData["TipoMensaje"]?.ToString() == "success" ? "check-circle" : "exclamation-triangle") me-2"></i>
                            @TempData["Mensaje"]
                        </div>
                        <button type="button" class="btn-close btn-close-white me-2 m-auto"
                                data-bs-dismiss="toast"></button>
                    </div>
                </div>
            </div>
        }

    <main class="content">
        @if (TempData["Bienvenida"] != null)
        {
            <div id="toast-bienvenida"
                 style="position:fixed;bottom:28px;right:28px;z-index:9999;
                background:linear-gradient(135deg,#1a5276,#2e86c1);
                color:#fff;border-radius:16px;padding:18px 24px;
                box-shadow:0 8px 32px rgba(26,82,118,.35);
                display:flex;align-items:center;gap:14px;
                max-width:360px;animation:slideIn .4s ease">
                <div style="width:42px;height:42px;background:rgba(255,255,255,.15);
                    border-radius:50%;display:flex;align-items:center;
                    justify-content:center;flex-shrink:0">
                    <i class="fas fa-hand-wave" style="font-size:1.1rem"></i>
                </div>
                <div>
                    <div style="font-weight:700;font-size:.88rem;margin-bottom:2px">
                        ¡Bienvenido!
                    </div>
                    <div style="font-size:.78rem;opacity:.88">
                        @TempData["Bienvenida"]
                    </div>
                </div>
                <button onclick="document.getElementById('toast-bienvenida').remove()"
                        style="background:none;border:none;color:#fff;cursor:pointer;
                       margin-left:auto;opacity:.7;font-size:1rem;padding:0">
                    <i class="fas fa-times"></i>
                </button>
            </div>

            <style>
                @@keyframes slideIn {
                    from {
                        transform: translateX(120%);
                        opacity: 0;
                    }

                    to {
                        transform: translateX(0);
                        opacity: 1;
                    }
                }
            </style>

            <script>
                setTimeout(function () {
                    var t = document.getElementById('toast-bienvenida');
                    if (t) {
                        t.style.transition = 'opacity .5s, transform .5s';
                        t.style.opacity = '0';
                        t.style.transform = 'translateX(120%)';
                        setTimeout(function () { if (t) t.remove(); }, 500);
                    }
                }, 4500);
            </script>
        }
        @RenderBody()
    </main>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-cerrar toast
        var toastEl = document.querySelector('.toast');
        if (toastEl) {
            setTimeout(function () {
                var t = bootstrap.Toast.getOrCreateInstance(toastEl);
                t.hide();
            }, 4000);
        }
    </script>
    @RenderSection("Scripts", required: false)
    <!-- Modal Logout -->
    <div class="modal fade" id="modalLogout" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" style="max-width:380px">
            <div class="modal-content" style="border-radius:18px;border:none;overflow:hidden">
                <div class="modal-body text-center p-4">
                    <div style="width:64px;height:64px;background:#fde8e8;border-radius:50%;
                            display:flex;align-items:center;justify-content:center;
                            margin:0 auto 16px">
                        <i class="fas fa-sign-out-alt" style="font-size:1.6rem;color:#e74c3c"></i>
                    </div>
                    <h5 style="font-weight:700;color:#1a5276;margin-bottom:8px">¿Cerrar sesión?</h5>
                    <p style="font-size:.85rem;color:#7f8c8d;margin-bottom:24px">
                        Tu sesión activa se cerrará. Tendrás que volver a iniciar sesión para acceder al sistema.
                    </p>
                    <div class="d-flex gap-2 justify-content-center">
                        <button type="button"
                                style="border:2px solid #dee2e6;background:#fff;border-radius:10px;
                                   padding:9px 24px;font-size:.85rem;font-family:'Poppins',sans-serif;
                                   font-weight:500;color:#555;cursor:pointer"
                                data-bs-dismiss="modal">
                            Cancelar
                        </button>
                        @using (Html.BeginForm("Logout", "Account", FormMethod.Post,
                                new { id = "formLogout" }))
                        {
                            @Html.AntiForgeryToken()
                            <button type="submit"
                                    style="background:linear-gradient(135deg,#e74c3c,#c0392b);
                                       color:#fff;border:none;border-radius:10px;
                                       padding:9px 24px;font-size:.85rem;
                                       font-family:'Poppins',sans-serif;
                                       font-weight:600;cursor:pointer">
                                <i class="fas fa-sign-out-alt me-2"></i>Sí, cerrar sesión
                            </button>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function confirmarLogout() {
            var modal = new bootstrap.Modal(document.getElementById('modalLogout'));
            modal.show();
        }
    </script>
    <!-- ── ASISTENTE FLOTANTE ── -->
    <div id="chatFloatContainer" style="position:fixed;bottom:24px;right:24px;z-index:9998;display:flex;flex-direction:column;align-items:flex-end;gap:10px;">

        <div id="chatFloatBox" style="display:none;width:340px;background:#fff;border-radius:18px;box-shadow:0 16px 50px rgba(0,0,0,.18);flex-direction:column;">
            <!-- Header -->
            <div style="background:linear-gradient(135deg,#1a5276,#2e86c1);padding:14px 16px;display:flex;align-items:center;justify-content:space-between;border-radius:18px 18px 0 0;">
                <div style="display:flex;align-items:center;gap:10px;">
                    <div style="width:34px;height:34px;background:rgba(255,255,255,.2);border-radius:10px;display:flex;align-items:center;justify-content:center;">
                        <i class="fas fa-robot" style="color:#fff;font-size:.95rem;"></i>
                    </div>
                    <div>
                        <p style="margin:0;font-size:.82rem;font-weight:700;color:#fff;">Asistente IA</p>
                        <span style="font-size:.65rem;color:rgba(255,255,255,.7);">LLaMA 3 · Semilleros</span>
                    </div>
                </div>
                <div style="display:flex;gap:8px;">
                    <a href="@Url.Action("Index","Asistente")" title="Pantalla completa"
                       style="width:28px;height:28px;background:rgba(255,255,255,.15);border-radius:7px;display:flex;align-items:center;justify-content:center;color:#fff;text-decoration:none;">
                        <i class="fas fa-expand-alt" style="font-size:.75rem;"></i>
                    </a>
                    <button onclick="cerrarChat()" style="width:28px;height:28px;background:rgba(255,255,255,.15);border:none;border-radius:7px;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;">
                        <i class="fas fa-times" style="font-size:.75rem;"></i>
                    </button>
                </div>
            </div>

            <!-- Mensajes -->
            <div id="floatMsgs" style="height:280px;overflow-y:auto;padding:14px;display:flex;flex-direction:column;gap:10px;background:#f8fafc;">
                <div style="display:flex;gap:8px;align-items:flex-start;">
                    <div style="width:28px;height:28px;background:linear-gradient(135deg,#4285f4,#34a853);border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
                        <i class="fas fa-robot" style="color:#fff;font-size:.7rem;"></i>
                    </div>
                    <div style="background:#fff;padding:9px 12px;border-radius:4px 12px 12px 12px;font-size:.78rem;color:#333;max-width:80%;box-shadow:0 1px 4px rgba(0,0,0,.06);">
                        ¡Hola! ¿En qué te puedo ayudar con tu semillero?
                    </div>
                </div>
            </div>

            <!-- Input -->
            <div style="padding:10px 12px;border-top:1px solid #f0f4f8;display:flex;gap:8px;background:#fff;border-radius:0 0 18px 18px;">
                <input type="text" id="floatInput" placeholder="Escribe aquí..."
                       style="flex:1;padding:8px 12px;border:2px solid #dee2e6;border-radius:9px;font-family:'Poppins',sans-serif;font-size:.78rem;outline:none;transition:border-color .2s;"
                       onkeydown="if(event.key==='Enter') enviarFlotante()"
                       onfocus="this.style.borderColor='#2e86c1'"
                       onblur="this.style.borderColor='#dee2e6'" />
                <button onclick="enviarFlotante()"
                        style="width:36px;height:36px;background:linear-gradient(135deg,#1a5276,#2e86c1);border:none;border-radius:9px;color:#fff;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;">
                    <i class="fas fa-paper-plane" style="font-size:.75rem;"></i>
                </button>
            </div>
        </div>

        <!-- Botón FAB -->
        <button onclick="toggleChat()" id="fabBtn"
                style="width:54px;height:54px;background:linear-gradient(135deg,#1a5276,#2e86c1);border:none;border-radius:50%;color:#fff;font-size:1.2rem;cursor:pointer;box-shadow:0 6px 22px rgba(26,82,118,.45);display:flex;align-items:center;justify-content:center;transition:transform .2s;"
                onmouseover="this.style.transform='scale(1.1)'"
                onmouseout="this.style.transform='scale(1)'">
            <i class="fas fa-robot" id="fabIcon"></i>
        </button>
    </div>

    @Html.AntiForgeryToken()

    <script>
    var chatAbierto = false;
    var floatEnviando = false;

    function abrirChat() {
        chatAbierto = true;
        document.getElementById('chatFloatBox').style.display = 'flex';
        document.getElementById('fabIcon').className = 'fas fa-times';
        document.getElementById('floatInput').focus();
    }

    function cerrarChat() {
        chatAbierto = false;
        document.getElementById('chatFloatBox').style.display = 'none';
        document.getElementById('fabIcon').className = 'fas fa-robot';
    }

    function toggleChat() {
        if (chatAbierto) cerrarChat();
        else abrirChat();
    }

    function agregarMsgFlotante(tipo, texto) {
        var box  = document.getElementById('floatMsgs');
        var isIA = tipo === 'ia';
        var wrap = document.createElement('div');
        wrap.style.cssText = 'display:flex;gap:8px;align-items:flex-start;' + (isIA ? '' : 'flex-direction:row-reverse;');
        var colorAvatar = isIA ? 'linear-gradient(135deg,#4285f4,#34a853)' : 'linear-gradient(135deg,#1a5276,#2e86c1)';
        var iconAvatar  = isIA ? 'robot' : 'user';
        var bRadius     = isIA ? '4px 12px 12px 12px' : '12px 4px 12px 12px';
        var bBg         = isIA ? '#fff' : 'linear-gradient(135deg,#1a5276,#2e86c1)';
        var bColor      = isIA ? '#333' : '#fff';
        wrap.innerHTML =
            '<div style="width:28px;height:28px;background:' + colorAvatar + ';border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;">' +
                '<i class="fas fa-' + iconAvatar + '" style="color:#fff;font-size:.7rem;"></i>' +
            '</div>' +
            '<div style="background:' + bBg + ';color:' + bColor + ';padding:9px 12px;border-radius:' + bRadius + ';font-size:.78rem;max-width:80%;box-shadow:0 1px 4px rgba(0,0,0,.06);word-break:break-word;line-height:1.5;">' +
                texto.replace(/\n/g, '<br>') +
            '</div>';
        box.appendChild(wrap);
        box.scrollTop = box.scrollHeight;
    }

    function mostrarTypingFlotante() {
        var box = document.getElementById('floatMsgs');
        var div = document.createElement('div');
        div.id = 'floatTyping';
        div.style.cssText = 'display:flex;gap:8px;align-items:flex-start;';
        div.innerHTML =
            '<div style="width:28px;height:28px;background:linear-gradient(135deg,#4285f4,#34a853);border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;">' +
                '<i class="fas fa-robot" style="color:#fff;font-size:.7rem;"></i>' +
            '</div>' +
            '<div style="background:#fff;padding:10px 14px;border-radius:4px 12px 12px 12px;box-shadow:0 1px 4px rgba(0,0,0,.06);display:flex;gap:5px;align-items:center;">' +
                '<div style="width:7px;height:7px;background:#aaa;border-radius:50%;animation:floatBounce .9s infinite;"></div>' +
                '<div style="width:7px;height:7px;background:#aaa;border-radius:50%;animation:floatBounce .9s infinite;animation-delay:.2s;"></div>' +
                '<div style="width:7px;height:7px;background:#aaa;border-radius:50%;animation:floatBounce .9s infinite;animation-delay:.4s;"></div>' +
            '</div>';
        box.appendChild(div);
        box.scrollTop = box.scrollHeight;
    }

    function enviarFlotante() {
        if (floatEnviando) return;
        var input   = document.getElementById('floatInput');
        var mensaje = input.value.trim();
        if (!mensaje) return;
        floatEnviando = true;
        input.value   = '';
        agregarMsgFlotante('user', mensaje);
        mostrarTypingFlotante();
        $.ajax({
            url: '@Url.Action("Preguntar","Asistente")',
            method: 'POST',
            data: {
                mensaje: mensaje,
                __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').first().val()
            },
            success: function (r) {
                var t = document.getElementById('floatTyping');
                if (t) t.remove();
                agregarMsgFlotante('ia', r.respuesta || ('⚠️ ' + (r.error || 'Error desconocido')));
            },
            error: function () {
                var t = document.getElementById('floatTyping');
                if (t) t.remove();
                agregarMsgFlotante('ia', '⚠️ No se pudo conectar con el asistente.');
            },
            complete: function () { floatEnviando = false; }
        });
    }
    </script>

    <style>
        @@keyframes floatBounce {
            0%, 60%, 100% {
                transform: translateY(0);
            }

            30% {
                transform: translateY(-7px);
            }
        }
    </style>
</body>
</html>